"""Live provider tests.

Everything in this package requires real credentials, a real device, or both,
and is excluded from the default pytest run. Run it explicitly:

    pytest -m live_api

See ``docs/acceptance-guide.md`` for the environment the live run expects.
"""
