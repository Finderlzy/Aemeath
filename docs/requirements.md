# Aemeath 需求

更新：2026-09-14。本文整理用户已表达的需求；建议和待定项分别标注。首期已有实现，最新交付证据见 [交付记录](delivery.md)。本轮 v2 范围见文末；情绪互动、音量检查与微信代答仍属于后续规划，尚未实现或验收。

## 目标与角色

面向用户个人 Windows 电脑的长期 AI 伙伴。以桌面为家，能够观察、聊天、记忆、主动行动和实际操作电脑。QQ、微信只是未来的互动渠道。

独立于“大肥鱼”，不继承其角色、记忆或账号状态。角色原型为《鸣潮》爱弥斯；支持简单默认人设和可选角色预设。具体角色设定、声音与美术资产尚未确定，不编造原作设定。

## 已确认首期

用户已接受：桌面角色、语音与文字聊天、记忆、屏幕感知、主动搭话。

- 用户工作时，她可结合屏幕内容主动语音搭话；用户可点击输入框打字，也可直接说话。
- 用户告知“我在上课”后进入课堂状态，后续使用文字交流；打字本身不等于要求永久静音。
- 可像同事摸鱼一样闲聊、接续旧话题、适时主动发起话题，而非只回答任务。
- 在相处中积累对用户的了解，形成持续关系。

建议首期验收：完成一次屏幕驱动的主动搭话与两种输入方式的接续对话；重启后召回用户明确提供的事实；课堂状态下不播放普通回复和主动搭话语音；用户忙碌或不回应时不连续追问。记忆纠正、删除、语音打断、主动频率参数为建议能力，未单独确认。

## 完整愿景，分期实现

1. 作息：根据时间和上次互动表现睡醒、困倦和埋怨；夜间走到床上盖被子。关机后下次启动推算状态，不需要关机期间运行。具体时间、睡眠时是否接受打扰待定。
2. 课堂监督：区分查资料与走神，保存用户正在做的内容，关闭相应活动，发“认真听课！”及表情包。保存需检查结果；无法确认时如何处理待定。
3. 维护用户：理解 QQ/微信聊天中的诋毁，自主生成并向对方发送角色语音。须区分本地播放与原生语音消息；具体平台能力、联系人范围与误判处理待验证。
4. 提醒吃药：记住用户提供的安排，到时提醒；建议独立记录提醒与服用确认，不从未回复推定已服用。具体时间与内容待配置。
5. 电脑行动：用户希望赋予较大自主权，角色应结合价值判断和上下文决定行动。具体执行范围尚未细化；本条是产品设计目标，不授权开发期间发送真实消息或关闭用户应用。

## 新增场景：情绪互动与消息代答

以下场景及行为边界于 2026-09-13 确认，属于后续范围，不加入首期完成条件。具体实现方案和实施顺序尚未确定；角色表现、状态判断与电脑行动仍由统一协调逻辑处理。

### F01：生气后躲起来

用户在对话中惹她生气时，她可以转身、躲到屏幕边缘、暂时减少回应，表达“不想理你”；用户安抚后逐步恢复交流。情绪是可恢复的角色状态，不能使功能失联：明确求助、停止指令和重要提醒仍须响应。

验收时应观察到躲藏、减少回应和安抚后恢复的过程，并验证躲藏期间仍能处理明确求助与停止指令、正常送达重要提醒。触发尺度、持续时间、自然恢复方式和具体动画待设计。

### F02：没有回应时检查声音并切换交流方式

她说话后没有得到回应时，先结合刚才的对话、用户是否离开、课堂或专注状态判断原因，不直接认定用户故意不理她，也不连续追问。可检查系统与应用静音状态、音频输出设备是否变化，并以“跑到音量图标旁看看”等动作表现检查过程；检测不可用时不声称已经确认原因。

发现声音关闭，或判断当前不适合说话时，改用文字交流，例如“原来你把声音关了呀”。尊重用户关声音的选择，不自行恢复音量；课堂状态持续约束输出，不因输入方式或情绪变化解除静音。

确认适合互动时，可以探头、敲屏幕边缘或发文字气泡吸引注意，并带点情绪问“为什么不理我？”。允许语音且适合播放时可同时使用文字和语音。最小化用户当前窗口仅作为用户主动开启的“允许闹脾气”玩法，限定可操作的应用和场景，执行后核对目标及结果；默认不开启。

验收覆盖静音、课堂、用户离开、正常可交流及音量检测不可用的分支：不得把未回应当成故意忽视，不连续追问，不自行开声音；未开启玩法或窗口不在允许范围内时不最小化。无回应等待时间、专注与离开的判断方式、提醒频率和可操作应用范围待设计。

### F03：微信联系人着急找用户时代答

用户微信挂着，某联系人连续发来消息；她结合消息内容和刚才的聊天，判断对方是否急着找用户。消息数量仅作为线索，不能单凭连续消息决定回复。知道用户去向不等于获准向他人披露，代答须有一次性交代，或针对指定联系人及信息范围的长期授权。

例如，用户出门前说“我去取快递了，如果小王找我，你帮我说一下”。小王随后来找时，她可回复：“我是 Aemeath，他刚才说去取快递了，回来后我会提醒他看消息。”身份声明放在开头，只使用有依据且在授权范围内的信息，不推测去向、返回时间，也不替用户答应事情。没有相应授权时不自动代答。

默认采用文字；预先允许语音代答时，可使用她自己的声音发送，并同样先表明身份。微信原生语音消息与本地语音播放是不同能力，平台发送能力须单独验证。用户回来后，告知谁来找过及实际回复内容；联系人、授权依据、回复内容与发送结果保存在本地。

验收先用隔离会话或替身覆盖：有授权时准确代答并声明身份，无授权、联系人不匹配或信息超出范围时不发送，不编造信息或承诺；核对实际发送结果并验证回报与本地记录。随后再在明确授权的真实会话中分别验收文字和原生语音。授权有效期与撤销方式、重复消息处理、发送失败处理及微信接入方案待设计。本条不授权开发验证期间向真实联系人发送消息。

## 约束与技术偏好

- 对话等模型计划走 API，用户担心本地模型效果。
- 记忆必须本地持久化：聊天历史、用户事实、经历摘要、表达与黑话、角色状态、提醒及检索索引均存放在用户电脑，不使用云端记忆托管、远程向量数据库或自动云同步。
- 已确认本地存储与模型部署分离：提取、总结、嵌入等模型处理可走 API，仅发送本次处理所需内容，结果回写本地。调用 API 会向提供商发送输入，不代表提供商必定零留存；选定服务时核对其数据留存选项。无需为记忆功能强制运行本地模型。
- 借鉴 MaiBot 的人设、记忆、表达与黑话学习，优先评估现有可复用技术。
- 云端屏幕/语音处理的具体范围、API 提供商和月预算未确定。API 模型需要发送对应输入，本地存储不等于输入不会离开电脑。
- 对话与提取的提供方可配置；实际在用的本机服务（EasyCLIProxyAPI）只改变请求出口，不改变上一条边界，也不构成零留存承诺。
- 不把大肥鱼的数据库、私聊、凭据或运行配置复制进新项目。

## 当前阶段边界

首期不要求完成通用电脑操作、QQ/微信代发、完整卧室动画，以及 F01–F03 的情绪躲藏、音量检查、窗口最小化玩法和微信代答。这些仍属于后续目标，不删除需求；本次不改变既有首期验收标准。
已确认优先以 Open-LLM-VTuber 验证桌面与语音能力，连接独立角色和本地记忆服务。

首期实现状态与验收证据见 [验收记录](acceptance.md)，交付结论与未解决项见 [交付记录](delivery.md)。两者已随 #1–#6 的关闭更新，不再只记录嵌入阻塞、语音与主动交流未完成的中间状态；实际通过项与延期项以交付记录的验收矩阵和未解决项为准。局部通过不能替代整体验收，延期项不得记为通过。

## 需求标识与验收对应

以下 R/E 编号用于追踪原有首期范围；新增后续场景使用上文 F01–F03 编号单独追踪。轮数、延迟和数据集判据沿用
[验收操作指南](acceptance-guide.md) 与 [真实验收记录](acceptance-live.md)，属于现有工程验收口径。

| 标识 | 已有范围与规则 | 可观察的完成标准 |
| --- | --- | --- |
| R01 | Windows 桌面角色，文字与语音聊天；ASR 首期目标为本地 SenseVoice，TTS 正式方案为本地 GPT-SoVITS | 实际客户端可启动，文字、麦克风输入均能得到可见／可听回复；ASR 由本地 SenseVoice 驱动，语音由 GPT-SoVITS 合成并真实播放；浏览器文字通过不等于桌面语音通过 |
| R02 | 课堂状态持续约束输出 | 输入“我在上课”后普通回复及主动搭话仅文字，后续打字不解除静音；重启保持按现有方案验证 |
| R03 | 本地记忆与持续关系 | 事实及来源留在本地；重启后用不同问法召回；同一会话历史回忆不能替代长期检索 |
| R04 | 屏幕感知 | 描述当前前台窗口，切换不串画面；关闭后停止观察且不保留迟到摘要；锁屏及窗口不可用有明确结果 |
| R05 | 结合情境主动搭话 | 不点击观察或发送搭话信号也能结合有效屏幕信息开口；用户打字／说话时不抢话；不回应后不连续追问 |
| R06 | 本地数据边界 | 聊天、记忆和索引本地存储，无远程记忆托管或自动同步；模型调用仅发送所需输入 |
| E01 | 已实现的建议能力：打断、纠正、遗忘与频率配置 | 按既有验收清单验证；保持“建议能力”来源，不因已有代码改成新确认需求 |

术语统一见 [项目术语](../context.md)。屏幕摘要只有在观察开启、来源窗口及有效期满足约束时才能进入对话。
实现不符时登记缺陷，不修改完成标准迁就实现。

**ASR 首期目标调整（2026-09-13 用户确认）**：ASR 首期目标改为**本地 SenseVoice**（基于 sherpa-onnx 本地离线运行），不再受外部 API 限制。**原有识别准确率、打断、静音及延迟验收要求全部保留**。
**TTS 已由用户确认调整范围**（2026-09-13）：正式方案为**本地 GPT-SoVITS**，
通过 localhost HTTP 接入，不再要求 TTS 使用云端 API。理由与边界：

- 目标是**固定角色音色**（角色原型爱弥斯的音色），GPT-SoVITS 的零样本音色克隆
  与后续专属训练能力符合该目标；云端 API TTS 难以固定到指定音色。
- 语音推理与语音转写均在用户电脑上执行，完全处于**本机数据边界内**：麦克风音频、转写文本、参考音频、合成结果与模型文件都不离开本机。引用音频与模型文件按本地资源管理，不进源码版本管理。
- **这不放宽验收**：本地 SenseVoice 与 GPT-SoVITS 必须实际通过转写、合成、播放、打断与课堂静音的验收，不能用"模型/服务能启动"替代。延迟按现有 `docs/acceptance-guide.md` 口径测量。
- **音色本身不在本次完成条件内**：参考音频暂缺，先不做角色音色与听感验收；爱弥斯正式音色与是否训练专属模型另行确定，不编造原作设定。

## v2 增量需求（2026-09-14）

用户已同意本轮推进管理界面与桌面常驻至可运行版本，允许分阶段交付；随后确认完整声音训练向导及自动学习方案。这里的 v2 是本轮迭代名称，未修改软件版本号，也不表示已发布。原有对话、打断、课堂静音、屏幕观察与本地记忆约束继续有效。调研依据见 [复用调研](reuse-research.md)。下述产品范围已确认，技术验证和正式素材仍有缺口；实现尚未开始。

| 标识 | 用户确认的范围 | 验收方向（具体交互随设计细化） |
| --- | --- | --- |
| V2-01 | 提供独立管理界面，管理一个爱弥斯的模型、人设、声音、记忆与 Live2D | 可从界面查看和修改真实配置，保存结果与实际使用配置一致，失败能明确显示 |
| V2-02 | 模型提供商由用户选择，不绑定指定厂商 | 可以配置受支持协议的服务地址、凭据与模型，并验证对应能力；不把任意提供商都已兼容作为承诺。首个闭环复用现有 OpenAI-compatible 接入，其他协议适配后再标可用 |
| V2-03 | 提供默认声音，并通过分步向导使用 GPT-SoVITS 训练和应用自己的声音，保留上游高级入口 | 默认声音可试听和用于对话；向导覆盖素材、校对、训练、试听和应用，失败可定位到步骤。默认素材尚待核实 |
| V2-04 | 记忆支持查看、搜索、纠正与删除 | 对实际本地记忆生效，纠正和删除后检索结果与正文一致，重启仍保持；删除与备份恢复遵守已有架构约束 |
| V2-05 | 仅从用户与爱弥斯的对话自动学习表达和黑话，通过检查后自动应用，核心人设不自动改写 | 学习结果展示来源，可纠正、禁用和撤销；重启后保持。禁用或撤销的结果不再注入，也不能从同一证据立即重新学回 |
| V2-06 | 角色离开浏览器小页面，常驻 Windows 桌面，默认右下角 | 无浏览器边框的角色窗口能与实际后端完成文字、语音和打断流程；位置、鼠标穿透和托盘行为随设计明确 |
| V2-07 | 在 Live2D 下方、任务栏上方显示一行当前发言字幕，参考 Neuro 的呈现 | 显示当前发言，长回复如何分段及停留时间随设计明确；不能遮住任务栏或把待播放全文误作当前发言 |
| V2-08 | 当前没有正式 Live2D 模型，用户后续制作 | 管理界面支持后续接入模型；正式角色资产未提供不阻塞配置与桌面功能开发，开发占位资产不能描述为正式爱弥斯模型 |

### 人设与学习规则

用户明确所指的人设编辑三块是“身份、性格、回复风格”，界面采用这一划分，不将 MaiBot 的字段原样改名复制。身份描述她是谁，性格描述稳定倾向，回复风格描述措辞、语气和长度偏好。另设表达学习与黑话词典；这些学习流程均不得改写核心人设。

学习只使用本项目中用户与爱弥斯的对话作为上下文；提取结果必须带本地来源、适用语境，经去重、证据与人设一致性检查后才能启用。爱弥斯复述过的学习条目不能反过来充当新的独立学习证据。不读取大肥鱼或其他平台的聊天。提取失败、来源不足或检查失败时保留失败或候选状态，不影响普通聊天，不假称已经学会。

用户可编辑词义或表达、禁用条目、撤销某次自动学习。人工修改优先于后续自动结果；恢复禁用项由用户显式操作。来源被遗忘时，相关学习证据和衍生内容按同一遗忘边界处理。模型处理与本地存储边界沿用 R06。

验收覆盖学习新用语、适用场景下自然使用、不相关场景不强行使用、歧义词保留待澄清、人工纠正优先、禁用与撤销后停止使用、重启保持，以及学习服务不可用时正常聊天。效果测试使用独立样本与对话，不以“数据库多了一条记录”代替学习效果。

### 声音操作流程

用户采用完整向导方案，纳入 v2，可分阶段交付：导入素材 → 清理切分 → 校对文字 → 准备训练集与训练 → 试听 → 应用。提供建议参数与简单解释，高级参数折叠，保留原 GPT-SoVITS 页面入口。校对由用户检查音频与文字；不能以自动 ASR 成功代替素材准确。

任务需显示当前步骤、进度或明确的运行状态、可读错误与重试入口。训练任务支持停止；只能重试已确认可独立重复的步骤，不能承诺训练进程被终止后一定断点续训。新声音通过试听后由用户应用；训练失败或停止不替换当前可用声音。训练与实时 TTS 的显存争用须验证并给出实际可执行的资源安排。

默认声音应开箱可选择并能真实合成，但正式音色资源尚未核实；不能将示例音色冒称爱弥斯正式音色。参考音频、训练集、模型权重与记录留在本地，不进源码版本管理。验证完整链路需要可用素材和训练硬件，界面开发可先用隔离任务验证状态，但不能据此宣称真实训练通过。

### 桌面与字幕交互

默认右下角的透明角色窗口可拖动、缩放、隐藏；空白区域穿透鼠标，托盘提供管理界面、显示／隐藏、停止发言与退出入口。关闭管理窗口不结束桌面陪伴；退出应用须与隐藏区分。窗口放置按所在显示器的工作区域计算，显示器移除后回到可见区域。

发言字幕在模型下方、任务栏上方保持一行；长内容按短句接续显示，完整回复仍可在聊天记录查看。语音时字幕随实际播放片段推进，不提前展示后续整段；打断或取消后停止旧字幕更新。课堂或 TTS 不可用时仍显示文字，语音不可用需明确提示。字号、宽度与停留时间提供设置；具体初始值在 UI 实现中以实际可读性校准，不将逐字音素对齐作为本轮要求。

没有正式 Live2D 时提供明确标识的示例预览或占位；模型加载失败仍能聊天、播放语音和显示字幕。管理窗口与角色窗口不得重复开启麦克风、播放同一句语音或重复回执。

### 实施顺序与剩余技术缺口

本轮优化保留用户已确认的范围。以下为落实这些需求的设计约定，供实现和验收使用，不将技术选择写成用户额外要求。

**首批可用**包含模型、人设、声音预设、记忆、Live2D 配置和桌面字幕；**完整 v2**还需完成表达与黑话学习、声音训练向导及整体验收。允许先交付首批，不能据此宣布整个 v2 完成。v3 两项继续保留，不要求在 v2 预先实现通用行为引擎。

### 关键操作的完成判据

| 操作 | 成功应能观察到 | 失败与兼容边界 |
| --- | --- | --- |
| 更换模型提供商 | 连接测试显示所测能力，保存后显示生效状态；生效后实际请求使用新连接和模型 | 连通成功不代表视觉、嵌入等全部可用；测试不更改当前连接，失败保留编辑内容 |
| 更换嵌入模型 | 旧记忆正文保留，新索引构建完成后同义查询可召回 | 即使维度相同也不能混用两个模型的向量；重建中显示检索状态，失败保留原可用配置与索引 |
| 编辑三块人设 | 新配置实际进入下一次采用该配置的对话；人工字段可独立维护 | 旧人设原文可查看并保持原行为，未人工迁移前不自动拆分；旧、新版本不得重复注入 |
| 使用和训练声音 | 可直接试听已有声音；训练新声音走完整向导，产物试听后由用户应用 | 普通用户无需先训练才能说话；关闭管理页不取消训练，重开后可查看状态；训练失败不替换现有声音 |
| 纠正或遗忘 | 生效后新轮次不再从记忆与学习上下文引用被撤回内容，重启一致 | 操作前说明实际影响；已播放内容不能撤回。旧备份恢复必须提示可能恢复已遗忘内容，并执行既有恢复规则 |
| 学习表达和黑话 | 来源可追溯，在合适语境自然使用；人工编辑、禁用和撤销优先生效 | 不把她自己的复述当新证据；同词不同含义保留语境，不用覆盖旧词义解决歧义；失败不阻塞对话 |
| 关闭、隐藏与退出 | 关闭管理页继续陪伴；隐藏角色可从托盘恢复；退出结束本应用会话 | 训练仍运行时，退出前列出运行任务并明确处理选择；不得顺带停止用户另行启动的服务 |

以上使用隔离数据与真实入口验证。学习效果评估在实现前固定独立样例和判据，分别覆盖正确学习、语境使用、误学拒绝、撤销／遗忘及重启，记录结果与失败样例，不能只统计写入条数。

管理界面与桌面端可在共同接口确定后并行开发，先验收管理基础，再验收桌面集成；表达学习、训练向导分阶段接入。接口、任务依赖与技术验证见架构及实施计划。首批提供商协议范围、凭据存储、训练接入方式属于需在开发前具体化并验证的技术工作，不限定提供商名称；不承诺未经适配的任意 API 能直接使用。

正式 Live2D 制作已单列为与 v2 并行的 v2.1（见下文）；未完成模型不阻塞 v2。默认声音素材与训练硬件资源为真实验收依赖，尚未核实；不阻塞其他独立功能。v2 最初参考 Neuro 的字幕位置，后续 v2.1 又确认借鉴其娇小体格与动漫画风，以选定 v4 立绘为准，不要求像素级复刻 Neuro 角色。

## v2.1 正式 Live2D 制作与接入（2026-09-14）

用户明确要求将当前形象制作列为 **v2.1**，与 v2 并行开发并发布 GitHub 计划。已确认 [v4 立绘](images/live2d/aemeath-approved-v4.png)，保留爱弥斯粉发、金瞳、头饰及服装特征，采用娇小的正常动漫比例，不再制作 Q 版。其他生成候选已清理，原始参考仍留本地。平面定稿已完成；以下交付尚未完成。

| 标识 | 本轮范围 | 可观察验收 |
| --- | --- | --- |
| V21-01 | 锁定选定外观并验证可持续的美术制作链路 | 原稿可追溯；抠图有真实 alpha；分层保存、重开、Cubism 导入及最小导出实测可复现 |
| V21-02 | 制作可编辑分层与遮挡补画 | 默认叠图保持选定外观，五官／头发／肢体独立，轻微移动不露空洞；保留美术源工程与分层 PSD |
| V21-03 | 制作基础动态与运行模型包 | 呼吸、眨眼、口型、小幅转头／视线与头发摆动自然；真实导出模型可由现有客户端加载 |
| V21-04 | 经 v2 管理及桌面能力接入正式角色 | 实际选用、比例／位置设置、字幕、语音、停止与静音正确；缺模型或失败不阻塞交流；重启可恢复选择 |
| V21-05 | 完成模型与桌面整体交付验收 | 从源工程／导出包可复现安装，Windows 实际桌面尺寸和完整交互通过，记录未通过项 |

阶段划分与基础动作集是本次规划采用的实施方案，不把先前建议的全部表情或动作视为用户已确认需求。首轮不包含走路、盖被子、换装、自动生成新动作、摄像头真人动捕、精细音素口型或训练新声音。额外表情／动作在基础闭环后另行细化，不作为当前交付门槛。

v2.1 仅在正式角色集成阶段依赖 V2-T02／V2-T03；不等待 v2 声音训练或表达学习，也不给完整 v2 增加正式角色资产的反向阻塞。沿用 R01–R03 与 V2-06–V2-08 的语音、静音、记忆、桌面与缺模型边界。本轮发布计划不修改软件版本、不打标签、不表示模型已经完成。

### v3 保留范围

用户明确将共同追番场景及自主创造行为系统留到 v3 再讨论，本轮不细化或开发。保留原始场景：她在右下角背向用户、侧脸朝屏幕看番并交流；难过时从撕开的数码空间取出纸巾擦泪并收回。新行为可自行创造，用户后续修改表现。素材、生成能力、触发方式及验收均未定。
