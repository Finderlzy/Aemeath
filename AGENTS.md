# 仓库指南

## 项目依据与现状

Aemeath 是 Windows 桌面 AI 伙伴，角色原型为《鸣潮》爱弥斯，独立于“大肥鱼”。开始工作前读取 [需求](docs/requirements.md) 和 [技术架构](docs/architecture.md)，区分已确认范围、建议和后续待定项。开发前读取 [实施计划](docs/implementation-plan.md) 确认里程碑与总体顺序，再到 [Issues](https://github.com/Finderlzy/Aemeath/issues) 认领具体任务：单项任务的范围、依赖、状态与完成证据以 Issue 为准，实施计划不重复维护。

项目术语统一维护在 [context.md](context.md)。v2 增量范围见需求中的 v2 章节，复用依据见 [增量调研](docs/reuse-research.md)；待决策条目不能当作已确定实现要求，既有首期计划不代表 v2 已完成规划。

v2.1 为与 v2 并行的正式 Live2D 制作与接入，开工前读取实施计划中的 v2.1 章节及 [制作契约](docs/live2d-production.md)。用户已选定 v4 立绘；分层、绑定与运行验收尚未完成。资产制作不阻塞 v2，桌面集成单向依赖 v2 管理与桌面任务，单项状态以 Issues 为准。

首期底座为 **Open-LLM-VTuber v1.2.1**（固定在 `vendor/Open-LLM-VTuber`，不入版本管理）。
Aemeath 核心模块位于 `aemeath/`，通过 [四个补丁](docs/patches/README.md) **按序**接入。
客户端源码在 `vendor/Open-LLM-VTuber-Web`（同样不入版本管理），构建后部署到上游 `frontend/`。

> **TTS 是"全部使用外部模型 API"的唯一例外。** 正式语音方案为**本地 GPT-SoVITS**
> （本机 HTTP 服务，api_v2，默认 `http://127.0.0.1:9880/tts`），已由用户确认；
> 引擎随上游内置，Aemeath 负责配置与连通探针。`tts_model` 默认仍为 `edge_tts`，
> 切换到 `gpt_sovits_tts` 前必须先启动该服务，否则 TTS 不可用（须明确报错，
> 不得静默退回其他引擎）。已有本地参考素材与试听记录，正式纯净角色音色留待后续。
> **ASR 首期目标调整为本地 SenseVoice**（基于 sherpa-onnx 本地运行），
> 麦克风录音转写链路已实测可用。原有识别准确率、打断、静音及延迟验收要求全部保留。
> 详见 [需求](docs/requirements.md) 与 [架构](docs/architecture.md)。

> **导入上游必须用 `src.open_llm_vtuber.*`。** 上游是脚本方式启动的，
> 自身模块加载为 `src.open_llm_vtuber.*`；若以 `open_llm_vtuber.*` 导入同一批文件，
> 会得到**两个不同的类对象**，`isinstance` 判定失败，模型生成的回复会被静默丢弃。
> 这个缺陷只会在真实链路暴露，测试全绿也可能复现。

已实现并有自动化测试覆盖：角色对话与打断、情境状态与课堂静音、本地事实与经历记忆、
屏幕观察边界、屏幕驱动的主动搭话调度、运行时装配与用量记录。测试入口见 [启动手册](docs/runbook.md)：

```powershell
.\vendor\Open-LLM-VTuber\.venv\Scripts\python.exe -m pytest
```

`tests/integration/` 下的测试经过**生产入口**（配置 schema、AgentFactory、桥接层、
上游输出处理、客户端协议），只替换模型、音频设备与捕获端；新增修复应优先在此留回归测试。
`tests/live/` 是需要真实凭据的连通测试，默认排除，用 `-m live_api` 显式运行。

**已实测通过**：DeepSeek 真实对话与记忆提取往返、浏览器驱动 10 轮中文对话、
屏幕真机捕获（前台窗口取景、不落盘）、EasyCLIProxyAPI 视觉接入与真实屏幕理解
（三类窗口描述准确、切换不串画面、迟到摘要被拒、不落盘）；
**本地嵌入服务接入与真实长期记忆（T04 闭环，#5 closed）**：通过本机 LM Studio 成功接入 `text-embedding-bge-large-zh-v1.5`（1024 维中文向量），校准集阈值扫描锁定下限 `0.40`；长期记忆三种状态（未启用/失败/无命中）语义彻底区分，生产入口回归测试全绿；验收集实测重启后同义召回 10/10 PASS、无关过滤 9/10 PASS、事实纠正 5/5 PASS、精确遗忘 5/5 PASS。
**真实语音闭环与采样可信度改造（T05 闭环，#6 closed）**：ASR 目标确认并实测为本地 SenseVoice，TTS 为本地 GPT-SoVITS；修复麦克风输入来源标记与客户端单调时钟计时器，有效制止样本判定生效；三项延迟指标实测全部达标（首段文字 64 样本 P95 1578ms ≤5s；语音回复 5 样本 P95 4755ms ≤10s；点击停止 5 样本 P95 0.8ms ≤500ms）。
**主动交流与交付复现推进（T06 / T07，#7 / #8 open）**：T06 替身报告已标定为隔离测试，真实打断与一次送达防重生效；T07 完整核心链路已在真实浏览器与服务重启中全链路走通（输入 → 事实提取 → 课堂静音 → 重启保持 → 长期记忆命中召回 → 下课恢复语音）；README 补齐实际运行与本地服务全流程，启动脚本加严（exit 1 超时退出），4+1 补丁干净复现通过。
**v2 管理基础（V2-T01 #14 closed；V2-T02 #15 closed；V2-M1 里程碑完成）**：V2-T01 交付管理 API 的配置读写、修订语义与凭据处理；V2-T02 在此基础上补齐**声音、记忆与 Live2D** 三个管理页，后端新增 `aemeath/management/{memory_admin,voices,live2d}.py`，前端并入补丁 `web/0002`。真实服务端到端探针 23/23、记忆生命周期 16/16 通过（含「遗忘只移除目标片段、同一条消息的其他内容保留」）；全量回归 `460 passed, 7 deselected`。首批可用的双窗口集成验收仍归 V2-T03，尚未执行。
GitHub #1–#6 已关闭；用户于 2026-09-13 明确将 #7 的现场锁屏采录与 #8 的两次 30 分钟真人试用延期至上线后反馈，不再阻塞当前交付和上线。延期不等于实测通过，#7–#8 保留跟进。最新详细实跑证据见 [验收汇总](docs/acceptance.md#十五首期验收缺口修复与收口推进记录2026-09-13)，
整体交付验收结论、验收矩阵与未解决项见 [交付记录](docs/delivery.md)。
不得把文档描述或替身测试写成真实场景验收结果。

## 模块与数据边界

计划目录职责见架构文档；按实施需要创建，不创建空占位目录。`docs/` 存放项目依据，后续代码、接口、测试和资源分别按架构组织。

记忆正文、聊天历史、用户事实、角色状态、表达与黑话、提醒、检索索引和备份必须本地存储。不使用云端记忆托管、远程向量库或自动云同步。模型提取、总结和嵌入可走 API，仅发送所需输入并回写本地；本地存储不等于提供商零留存。计划本机数据位于 `data/`，备份位于 `data/backups/`，实现时排除出源码版本管理。

仅参考大肥鱼的技术，不复制其角色记忆、账号凭据或运行配置，不改变其服务状态。借用 MaiBot 代码前检查依赖与许可证，不直接复制耦合模块。

## 行为与验证

课堂状态将回复限制为文字输出，不能因为用户输入方式变化而丢失静音状态。主动聊天、用户回复、作息与电脑操作由统一协调逻辑处理。提醒时间与完成状态应持久化，不依靠模型偶然召回。

较大自主权是产品目标，不代表开发验证可以擅自向联系人发消息或关闭真实工作窗口。保存与关闭必须验证结果；聊天平台发送能力和本地语音播放分别验收。

纯文档改动检查链接、事实、一致性和空白；实现后按首期场景验证语音、静音、记忆和主动交流。具体测试框架及命令在配置后记录，不虚构现有工具。后续电脑及消息动作先使用隔离窗口或替身验证。

## 维护约定

本层仅以 `AGENTS.md` 为规则权威入口，未指定其他 Agent 兼容入口。尚无项目代码格式或提交惯例；新增工具时记录实际入口。建议每次变更围绕一个可验收主题，说明行为变化、验证结果与限制；不自动提交。需求或架构变化时同步更新依据，始终区分计划与已实现功能。

### 密钥处理

**补丁、文档与配置中不得出现字面密钥，`${VAR}` 是唯一允许形式。** 提交前运行
`.\scripts\check-secrets.ps1`（发现疑似密钥返回非零退出码）；样例值走 `.env.example`。

生成补丁时尤其注意：`git diff` 会连带把代码里的字面密钥写进 `docs/patches/*.patch`。
2026-09-14 曾因此把真实 DeepSeek 密钥提交进 `main` 并触发 GitGuardian 告警，
完整经过见 [补丁说明](docs/patches/README.md)。已泄露的密钥必须**吊销轮换**——
仅删除字符串不足以消除风险，历史重写也无法撤回已被抓取的副本。
