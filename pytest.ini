[pytest]
# Aemeath test configuration.
#
# Default run excludes live API tests; they require real credentials and are
# selected explicitly with: pytest -m live_api
testpaths = tests
asyncio_mode = auto
addopts = -q --strict-markers -m "not live_api"
markers =
    live_api: requires real provider credentials; not run by default
