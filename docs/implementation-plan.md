# Aemeath 首期实施与验收计划

更新：2026-09-12。依据 [需求](requirements.md)、[架构复盘](architecture.md#2026-09-12-核心链路复盘)
和 [验收记录](acceptance.md)。本文件是当前本地任务入口；未采用 GitHub Issues，不另外发布任务或重复维护任务系统。

## 本轮结论与基线

首期已有功能实现，真实文字与视觉有通过记录；完整的“屏幕驱动主动语音 → 用户接续 → 课堂静音 → 重启记忆”尚未闭合。
先修接入缺口，再补真实体验证据。保留固定上游与本地数据边界，暂不扩展电脑操作、平台发送、卧室动画和学习模块。

基线为 2026-09-12 本机未提交工作树；上层仓库 HEAD `f7f634d` 不含全部当前改动。
本轮只修改文档并运行内存隔离探针，没有重跑既有 244 项测试或真实 API，也没有验收麦克风、扬声器和桌面安装产物。
历史验收的原始时间与指标存在口径冲突，T00 负责复核；历史通过记录在本轮仅作为已有证据引用。

## 需求覆盖

| 需求 | 当前依据 | 剩余缺口 | 对应任务 |
| --- | --- | --- | --- |
| R01 对话与桌面 | 浏览器 10 轮文字已有通过记录 | 桌面实际入口、麦克风、语音及可听主动输出 | T01、T05、T07 |
| R02 课堂 | 状态、输出闸门与隔离测试已实现 | 播放中切换、主动音频、重启保持真人验证 | T01、T05 |
| R03 本地记忆 | SQLite、提取与历史持久化有证据 | 可用嵌入、中文检索质量、重启后同义召回 | T04 |
| R04 屏幕 | 手动捕获与视觉已有通过记录 | 迟到缓存、关闭后的请求、锁屏与窗口不可用 | T02、T03、T06 |
| R05 主动交流 | 定时器与资格规则已实现 | 空音频、重复计数、屏幕上下文、生成中状态变化与恢复 | T01、T03、T06 |
| R06 本地边界 | SQLite、本地索引与忽略规则 | 各项接入和复现时持续核查，不引入云端记忆 | T02、T04、T07 |
| E01 建议能力 | 打断、纠正和精确遗忘已有隔离覆盖 | 真实打断、真实嵌入下纠正遗忘 | T04、T05 |

## 里程碑与任务

M1：屏幕驱动的主动交流链路接通（T01–T03），从 T01 开始；T00 同阶段校准证据。
M2：真实语音与长期记忆完成验收（T04–T06）。M3：实际交付入口与两次持续试用完成（T07）。
任务之间可独立准备供应商和验收材料，但共享桥接层与上游补丁按顺序集成。

| 任务 / 状态 | 输入、涉及边界与产物 | 依赖 | 完成标准与验证 |
| --- | --- | --- | --- |
| T00 证据复核 / 待做 | 当前文档、原始报告与轮次日志；更新既有验收文件 | 无 | 核对历史 2026-09-14 日期与当前日期冲突、518/1287 ms 口径、244 项测试对应基线；原始资料缺失则标注待核，不编造日期。进入修复前运行相称回归建立新基线 |
| T01 主动输出闭合 / 首个开发任务 | A01/A02/A05；`bridge.py`、`coordinator.py`、上游 `service_context` 与输出处理、补丁 0003；产物为接入修复及生产入口回归 | 无 | 一次候选共享可追踪轮次；正常模式生成非空可解码音频，课堂模式不合成／播放；只在首次有效显示回执计数一次。覆盖重复／丢失回执、失败发送、断线、用户插话、生成中关闭主动开关；实际上游生成器参与测试 |
| T02 屏幕生命周期 / 待做 | A03；`screen.py`、桥接开关与请求；产物为代次防护及回归 | 无；与 T01 串行合并共享桥接改动 | 延迟响应在关闭／重新开启后不能写入旧缓存或发送；关闭状态的手动请求也不能捕获；同时断言捕获调用次数、缓存与出站帧，确保前台窗口取景与不落盘 |
| T03 屏幕驱动主动对话 / 待做 | A04；runtime 调度、摘要有效性、Agent 提示词与上游生成器；产物为按需观察接入及集成验证 | T01、T02 | 使用隔离窗口，不发送客户端搭话或观察信号，也能自动引用具体窗口内容；普通模式有语音，课堂仅文字；生成前后检查开关、窗口、摘要时效与用户活动；关闭后不调用视觉、不引用旧摘要，重复画面遵守节流 |
| T04 真实记忆 / 外部依赖阻塞 | 支持嵌入的端点、模型与有效凭据；现有适配器、独立数据库、校准／验收集；产物为配置、必要修复与检索报告 | 可独立准备；真实验收依赖可用供应商 | 用校准集选下限后锁定，在独立验收集验证同义命中与无关空结果均 ≥9/10；再验证纠正、遗忘、重启召回与索引模型版本。未配置／失败与无命中分别可诊断。已有供应商不可用时不以替身或同一会话上下文冒充通过 |
| T05 真实语音与课堂 / 待做 | 实际 ASR/TTS 配置、麦克风／扬声器、客户端；操作步骤沿用验收指南 | T01；API 引擎验收依赖对应供应商 | 10 轮语音、5 次播放中打断、点击停止、播放中“我在上课”、课堂 10 轮、重启保持、关麦停采均完成记录；真实听感由人确认。沿用现有三项延迟目标与 ≥20 有效样本要求，不放宽判据 |
| T06 主动与失败恢复 / 待做 | T03/T05 产物、隔离窗口、连接与模型失败场景；更新现有验收记录 | T03；语音分支依赖 T05 | 用户忙碌或不回应时不连续追问；请求失败后可继续；断线重连无重复问候或旧输出；锁屏／窗口不可用有明确行为；一次发送一次计数在实际客户端成立 |
| T07 首期交付验收 / 待做 | M1/M2 产物、四个补丁、前端源码与构建部署流程、README | T00、T04、T05、T06 | 在干净临时目录验证实际启动入口、补丁与前端构建部署可复现，不强造安装包；补齐两次各 30 分钟人工试用和完整核心流程。保留必要证据并清理临时数据；通过、失败、阻塞、未执行分别报告 |

T01 建议执行顺序：先从真实上游主动生成器入口建立失败回归 → 统一输出／轮次与回执语义 → 接入 TTS → 核对上游副本与补丁 → 验证普通回复及课堂分支不回退。
每个实现任务使用 `task-implement`；M3 使用 `project-delivery`。本次规划完成不表示上述任务已完成或自动获准发布。

## 验收入口与决策边界

按 [启动手册](runbook.md) 使用项目虚拟环境；真实 API 与设备操作沿用 [验收指南](acceptance-guide.md)。
使用独立验收配置、数据库与日志。不得复制大肥鱼数据、改变其服务，或为验证关闭真实工作窗口、向联系人发消息。

可直接推进 T01–T03 的代码与隔离验证，不必等待嵌入供应商。T04 需要可用嵌入服务；T05 的 API 目标需要支持相应能力的服务。
现有 SenseVoice 与 edge-tts 可以用来定位设备／播放问题，但不能替代 API 引擎验收。
如要改变首期引擎目标，再集中确认该决策，不在实现任务里暗改需求。

正式角色声音、美术与月预算在对应选型前细化。后续作息、提醒、表达学习、电脑操作和 QQ/微信渠道在 M3 后按场景规划，保留需求但不提前拆到函数。
本轮不改变版本、不创建外部任务、不提交或推送，遵循项目“不自动提交”的约定。
